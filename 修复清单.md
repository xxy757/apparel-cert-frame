# 修复清单（按优先级）

1. ✅ P0 证书表缺失导致证书管理不可用（已补 `certificate` 表结构）
2. ✅ P0 消息中心未联调（补齐 `userId/userType` 参数与分页）
3. ✅ P0 岗位模型双轨字段不一致（统一企业端发布/管理字段口径，补 `job` 表字段）
4. ✅ P0 证书表结构重复（`certification_certificate` vs `certificate`）已提供迁移脚本与弃用标注
5. ✅ P1 企业认证/主页管理页面静态数据改为真实接口联调
6. ✅ P1 培训推荐页面静态数据改为真实接口联调
7. ✅ P1 人才搜索后端返回 `null`，已实现搜索逻辑
8. ✅ P2 管理端认证审核仍为 mock，已接入真实审核接口
9. ✅ P2 管理端用户统计接口缺失，已补接口并联调

## 备注

1. 运行已有数据库时，需要手动补充 `job` 表新增字段：`salary_min/salary_max/work_type/recruit_num/skills/is_urgent/certification_requirement`
2. `certificate` 与 `certification_certificate` 两套证书表并存，后续需决定保留哪一套并做数据迁移
